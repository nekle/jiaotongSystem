export default {

    getId(state) {
        return state.id
    },

    getName(state) {
        return state.name
    },

    getScene(state) {
        return state.scene
    },

    getPath(state) {
        return state.path
    },

    getDir(state) {
        return state.dir
    },

    getStatus(state) {
        return state.status
    }

}