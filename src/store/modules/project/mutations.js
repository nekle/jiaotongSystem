export default {

    setId(state, id) {
        state.id = id
    },

    setName(state, name) {
        state.name = name
    },

    setScene(state, scene) {
        state.scene = scene
    },

    setPath(state, path) {
        state.path = path
    },

    setDir(state, dir) {
        state.dir = dir
    },

    setStatus(state, status) {
        state.status = status
    }

}